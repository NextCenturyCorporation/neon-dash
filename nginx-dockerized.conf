server {
    listen 80;
    server_name neon-ui;
    root /var/www;
    index index.html;
    location / {
        try_files $uri /index.html;
    }
    location /neon {
        proxy_pass http://neon-server-service;
    }
}
