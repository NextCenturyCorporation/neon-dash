server {
    listen 80;
    server_name e2e-ui;

    root /var/www;
    index index.html;

    location /neon {
       proxy_pass http://e2e-neon-server:8090/;
    }

    # Force all paths to load either itself (js files) or go through index.html.
    location / {
        try_files $uri /index.html;
    }
}